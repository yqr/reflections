When would you want to use a remote repository rather than keeping all your work local?

When working with multiple people or multiple computers it's probably better to use a remote repository rather than a local repository. 
